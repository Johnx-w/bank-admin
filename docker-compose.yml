version: "3.8"

services:
  bank-admin:
    # 用当前目录的 Dockerfile 构建
    build: .
    # 宿主机 8080 → 容器 80
    ports:
      - "8080:80"
    # 崩了自动重启，除非手动 docker-compose down
    restart: unless-stopped
    # 容器名（方便 docker logs bank-admin 查看）
    container_name: bank-admin
